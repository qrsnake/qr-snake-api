import select from '../../database/Select';
const animalpost = (req, res) => {
  const { body } = req.body;
  /*
  const { id } = insert(body, 'animal');
  if(data == null){
    res.status(400).send(id);
  }
  else{
    res.status(201).send(id);
  }
  */
};

export default animalpost;
