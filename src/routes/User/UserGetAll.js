const usergetall = (req, res) => {
  /*
  const { data } = select();
  if(data == null){
    res.status(404).send(data);
  }
  else{
    res.status(200).send(data);
  }
  */
};

export default usergetall;
